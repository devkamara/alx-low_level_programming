#include <stdio.h>
#include <stdlib.h>

/**
 * main - prints alphabet in lowercase
 * Return: 0 if true (success)
 */

int main(void)
{
int ch;
for (ch = 'a'; ch <= 'z'; ch++)
{
if ch== 'e' || ch == 'q')
{
continue;
}
putchar(ch);
}
putchar ('\n');
return (0);

}
